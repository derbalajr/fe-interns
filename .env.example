VITE_API_URL=http://127.0.0.1:8000/api

# Onboarding agent (streaming chat + floor-map navigation).
# VITE_ONBOARDING_API_BASE = the address the BROWSER can reach (baked into floor_map URLs).
# The token is a single shared demo token and is visible in the browser — do not use in a public build.
VITE_ONBOARDING_API_BASE=http://localhost:8000
VITE_ONBOARDING_API_TOKEN=
